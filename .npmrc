# Next 15.0.3's declared peer for React is "^18 || 19.0.0-rc-...", but it
# works fine with stable React 19. next-mdx-remote and Auth.js v5 also have
# loose peers that npm 10's strict resolution rejects. Allow npm to use the
# transitive versions we have installed.
legacy-peer-deps=true
